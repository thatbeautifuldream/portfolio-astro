---
interface Props {
  title: string;
  meta?: string;
}

const { title, meta } = Astro.props;
---

<header class="grid gap-1">
  <h1 class="font-medium text-pretty">{title}</h1>
  {meta && <p class="text-faint">{meta}</p>}
  <slot />
</header>
